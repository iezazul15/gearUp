model Profile {
  id        String  @id @default(uuid())
  phone     String?
  address   String?
  avatarUrl String?
  bio       String?

  userId String @unique
  user   User   @relation(fields: [userId], references: [id])

  @@map("profiles")
}
